# Multi-tenant REST authorization benchmark v2

This corpus is generated from the executable ERSEC authorization case declarations. It is loopback-only, synthetic, GET-only, and state-preserving. Truth metadata requires independent operator review before any external benchmark claim.

Ambiguous cases are intentionally retained and excluded from precision/recall. The corpus digest in `manifest.json` identifies the exact case set.
